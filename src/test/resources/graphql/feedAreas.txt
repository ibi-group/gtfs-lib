query ($namespace: String) {
  feed(namespace: $namespace) {
    feed_version
    area(limit: 2) {
      area_id
      area_name
      id
      stopAreas {
        area_id
        stop_id
        id
        stops {
          stop_id
          stop_name
        }
      }
    }
  }
}